<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Keep Android 12+ on Flutter's standard launch background. This avoids
         platform splash attributes when a build uses an older compile SDK. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- <item name="android:windowSplashScreenBackground">@color/mobi_ink</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item> -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- <item name="android:postSplashScreenTheme">@style/NormalTheme</item> -->
    </style>
</resources>
